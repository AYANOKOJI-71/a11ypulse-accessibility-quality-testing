FROM node:22-bookworm-slim

WORKDIR /app
COPY apps/target/package.json ./
COPY apps/target/server.mjs ./

EXPOSE 5300
HEALTHCHECK --interval=20s --timeout=5s --start-period=5s --retries=3 CMD node -e "fetch('http://127.0.0.1:5300/demo-reviewed-baseline').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"
CMD ["node", "server.mjs"]
